<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Yelp-Camp</title>

        <link
            rel="stylesheet"
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
        />
        <link rel="stylesheet" href="stylesheets/styles.css" />
    </head>
    <body class="d-flex flex-column vh-100">
        <%- include('../partials/navbar') %>
        <main class="container mt-5">
            <%- include('../partials/flash') %>

            <!--? We are basically goint to keep injecting our content into this layout. -->
            <%- body %>
        </main>
        <%- include('../partials/footer') %>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <script src="/javascripts/validateForms.js"></script>
        <!-- by default this will look in 'public' dirctory, so no need for specific '/public/javascripts/validateForms.js' -->
        <script src="https://cdn.jsdelivr.net/npm/bs-custom-file-input/dist/bs-custom-file-input.min.js"></script>
    </body>
</html>

<!--note : Points to be noted -->

<!-- ? We can kinda say that this is an alternative to using partials. -->
<!-- ? As far as i know, both (the ejsMate and partials) boh have the same functionality but in different ways. -->
